module.exports = {
    host: 'host.docker.internal', // Postgres ip address[s] or domain name[s]
    port: 5432, // Postgres server port[s]
    database: 'Sa-Dev', // Name of database to connect to
    user: 'postgresql', // Username of database user
    password: 'postgresql',
}